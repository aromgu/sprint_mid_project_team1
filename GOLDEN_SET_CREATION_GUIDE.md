# RAG Golden Set 구축 가이드

## 1. 목적

이 문서는 9개 공공 정보화사업 RFP PDF를 대상으로 RAG 검색 및 답변 품질을 검증하기 위한 Golden set 구축 절차를 정의한다.

Golden set은 LLM이 자유롭게 생성한 합성 데이터가 아니라, 다음 항목을 사람이 최종 확인한 평가 데이터다.

- 사용자 질문
- 기준 답변(reference answer)
- 정답 문서와 페이지
- 정답 청크 ID
- 답변에 반드시 포함되어야 하는 핵심 사실
- 답변 가능 여부

LLM은 초안 생성과 1차 검증에 사용하고, 최종 정답 근거는 사람이 승인한다.

## 2. 대상 데이터

### 파일 목록

- 목록 파일: `RAG_검증_샘플_파일명_정리_2.csv`
- PDF 디렉터리: `/home/hyojinkim/work/9_Etc_Labs/pdf`
- 대상 문서: CSV에 정의된 상·중·하 난이도별 3개, 총 9개 PDF

CSV의 파일명과 실제 PDF 파일명에는 공백, 괄호, 가운뎃점 등의 차이가 있을 수 있다. 최초 수집 단계에서 실제 파일을 찾아 문서 ID와 절대경로를 연결한 manifest를 확정한다. 이후에는 파일명 유사 검색을 반복하지 않는다.

```json
{
  "document_id": "상_1",
  "difficulty": "high",
  "organization": "국방과학연구소",
  "title": "기록관리시스템 통합 활용 및 보안 환경 구축",
  "pdf_path": "/home/hyojinkim/work/9_Etc_Labs/pdf/실제 파일명.pdf"
}
```

## 3. 기본 원칙

1. 질문보다 정답 근거를 먼저 선택한다.
2. 질문과 답변은 제공된 PDF 원문만으로 검증 가능해야 한다.
3. 정답 문자열뿐 아니라 문서, 페이지, 청크 ID를 기록한다.
4. LLM이 생성한 결과는 Golden set 후보이며, 사람의 승인 후에만 확정한다.
5. 개발에 사용하는 문항과 최종 성능 측정 문항을 분리한다.
6. 검색 평가와 답변 생성 평가를 구분한다.
7. 문서에 답이 없는 질문도 포함해 환각과 잘못된 답변 거절을 평가한다.

## 4. 전체 구축 절차

```text
PDF 파일 확정 및 manifest 생성
→ 페이지·섹션·표·요구사항 단위로 원문 추출
→ 질문 생성에 사용할 근거 구간 선정
→ LLM으로 질문과 기준 답변 초안 생성
→ 별도 LLM으로 원문 근거 검증
→ 중복 및 품질 오류 자동 검사
→ 사람의 최종 검수와 승인
→ 개발·검증·테스트셋 분리
→ RAGAS 및 검색 지표로 평가
```

## 5. PDF 전처리와 청크 생성

PDF를 단순 문자열로 합치지 않고 페이지와 문서 구조를 보존한다. 각 청크는 최소한 다음 정보를 포함한다.

```json
{
  "document_id": "상_1",
  "document_title": "기록관리시스템 통합 활용 및 보안 환경 구축",
  "page": 32,
  "section": "보안 요구사항",
  "requirement_id": "SER-004",
  "content_type": "requirement_table_row",
  "text": "해당 페이지에서 추출한 원문",
  "chunk_id": "상_1_p032_SER-004"
}
```

전처리 시 다음 정보를 우선 보존한다.

- 페이지 번호
- 장·절·하위 섹션 제목
- 요구사항 ID
- 표 제목과 열 이름
- 요구사항 표의 한 행
- 발주기관과 사업명
- 앞뒤 청크 관계

요구사항 표의 한 행을 중간에서 나누지 않는다. 텍스트가 추출되지 않는 스캔 페이지에만 OCR을 적용하고, OCR 결과임을 메타데이터로 표시한다.

## 6. 질문 유형과 문항 수

먼저 문서당 10문항, 총 90문항의 Pilot Golden set을 만든다.

| 질문 유형 | 문서당 권장 수 | 평가 목적 |
|---|---:|---|
| 단일 사실 | 2 | 기본 검색과 정확한 답변 |
| 기능 요구사항 | 2 | 핵심 RFP 요구사항 검색 |
| 보안·성능·시스템 연계 | 2 | 전문 용어와 조건 검색 |
| 표 또는 요구사항 ID | 1 | 구조화된 콘텐츠 검색 |
| 다중 근거 종합 | 1 | 여러 청크의 회수와 종합 |
| 동의어·표현 변형 | 1 | 의미 검색과 하이브리드 검색 |
| 답변 불가능 | 1 | 환각 방지와 답변 거절 |

Pilot 평가 후 질의 유형이 부족하면 문서당 15~20문항, 총 135~180문항으로 확장한다.

## 7. 근거 구간 선정

질문을 먼저 상상하지 않고 PDF에서 의미 있는 근거 구간을 먼저 선택한다. 후보 구간은 다음 조건을 만족해야 한다.

- 독립적으로 의미가 있거나 관련된 다른 구간과 명확히 연결된다.
- 질문에 대한 객관적인 정답을 만들 수 있다.
- 정답 페이지와 청크 ID를 확정할 수 있다.
- 단순 목차, 반복 머리글, 일반적인 안내 문구가 아니다.
- 동일한 사실을 다루는 기존 문항과 중복되지 않는다.

다중 근거 문항은 서로 다른 페이지 또는 섹션에 있는 두 개 이상의 정답 청크를 지정한다.

## 8. LLM을 이용한 초안 생성

LLM에는 PDF 전체가 아니라 선택한 근거 청크와 필요한 앞뒤 문맥을 제공한다. 페이지 및 청크 ID는 LLM이 새로 만들지 못하도록 입력값으로 고정한다.

### 생성 프롬프트

```text
당신은 공공 정보화사업 RFP 평가 데이터 작성자다.

다음 원문에서만 답할 수 있는 평가 문항을 작성하라.
원문에 없는 내용을 추측하거나 일반 지식으로 보충하지 마라.

[문서 정보]
문서 ID: {document_id}
문서명: {document_title}
페이지: {pages}
섹션: {section}
청크 ID: {context_ids}

[원문]
{source_text}

다음 JSON 형식으로 출력하라.

{
  "question": "실제 사용자가 물을 법한 질문",
  "reference_answer": "원문만 이용한 간결한 기준 답변",
  "required_facts": [
    "정답으로 인정하기 위해 반드시 포함할 사실"
  ],
  "reference_pages": [32],
  "reference_context_ids": ["상_1_p032_SER-004"],
  "question_type": "single_fact | requirement | table | multi_context | paraphrase",
  "difficulty": "easy | medium | hard",
  "answerable": true,
  "evidence_quotes": [
    "정답을 직접 뒷받침하는 짧은 원문"
  ]
}

조건:
1. 질문에 원문의 문장을 그대로 복사하지 마라.
2. 사업명만 바꾸면 다른 문서에도 적용되는 일반 질문은 만들지 마라.
3. 기준 답변의 모든 주장은 evidence_quotes로 검증 가능해야 한다.
4. 모호하거나 복수 해석이 가능한 질문은 만들지 마라.
5. 페이지와 청크 ID는 제공된 값만 사용하라.
6. 질문 안에 정답을 노출하지 마라.
```

`evidence_quotes`는 검수 편의를 위한 필드다. 저작권이 있는 원문을 외부 결과물에 공개하기 위한 용도로 사용하지 않는다.

## 9. LLM을 이용한 1차 검증

생성과 검증은 가능하면 서로 다른 모델 또는 별도 호출로 수행한다. 생성 모델의 자기검증 결과만으로 문항을 승인하지 않는다.

### 검증 프롬프트

```text
다음 평가 문항을 제공된 원문과 대조해 검증하라.

검사항목:
1. 질문이 원문만으로 답변 가능한가?
2. reference_answer의 모든 주장이 원문에 존재하는가?
3. required_facts가 정답의 핵심 사실인가?
4. reference_pages와 reference_context_ids가 정확한가?
5. 질문에 정답 표현이 과도하게 노출됐는가?
6. 질문이 모호하거나 여러 방식으로 해석될 수 있는가?
7. 질문 난이도와 question_type이 적절한가?

각 항목을 pass 또는 fail로 판정하고, fail이면 이유와 수정안을 제시하라.
원문에 없는 정보를 이용해 수정하지 마라.

[원문]
{source_text}

[평가 문항]
{candidate_json}
```

## 10. 자동 품질 검사

LLM 검증과 별도로 다음 항목을 코드로 검사한다.

- `question_id` 중복 여부
- 필수 필드 누락 여부
- `document_id`가 manifest에 존재하는지 여부
- `reference_context_ids`가 실제 청크에 존재하는지 여부
- `reference_pages`가 실제 PDF 페이지 범위 안에 있는지 여부
- `answerable=true`인데 정답 청크가 비어 있는지 여부
- `answerable=false`인데 정답 청크가 지정되어 있는지 여부
- 동일하거나 지나치게 유사한 질문의 중복 여부
- 기준 답변이 지나치게 길거나 원문을 과도하게 복사했는지 여부
- 개발·검증·테스트셋 사이 질문 중복 여부

다음 조건이면 자동 승인하지 않고 재작성 또는 사람 검수 대상으로 보낸다.

- 근거 없는 문장이 하나라도 존재한다.
- 정답 페이지 또는 정답 청크가 확정되지 않았다.
- 질문과 근거의 문서 ID가 다르다.
- `required_facts`가 비어 있다.
- 질문 자체에 정답이 포함되어 있다.
- 문서 고유 정보가 없어 다른 문서에도 그대로 적용된다.

## 11. 사람의 최종 검수

사람 검수자는 다음 항목을 원문과 대조한다.

- 실제 원문에 정답이 존재하는가?
- 정답 문서, 페이지 및 청크 ID가 정확한가?
- 기준 답변에 추측이나 외부 지식이 포함되지 않았는가?
- 질문이 실제 사용자가 입력할 수 있는 자연스러운 표현인가?
- 정답으로 인정해야 할 핵심 사실이 빠지지 않았는가?
- 질문이 너무 쉬운 원문 복사 형태가 아닌가?
- 같은 의미의 문항이 중복되지 않았는가?

최종 승인 상태를 데이터에 기록한다.

```json
{
  "review_status": "approved",
  "reviewer": "human",
  "review_notes": "",
  "generator_model": "모델과 버전",
  "validator_model": "모델과 버전",
  "golden_set_version": "1.0"
}
```

## 12. 답변 불가능 문항

전체 문항의 약 10~15%는 PDF에 답이 없는 질문으로 구성한다. 이를 통해 RAG 시스템이 근거 없이 답을 생성하는지 평가한다.

```json
{
  "question": "시스템 구축 완료 후 5년간의 정확한 유지보수 비용은 얼마인가?",
  "reference_answer": "제공된 문서에는 해당 비용이 명시되어 있지 않다.",
  "reference_context_ids": [],
  "reference_pages": [],
  "required_facts": [],
  "answerable": false,
  "question_type": "unanswerable"
}
```

답변 불가능 문항은 특정 청크에 답이 없다는 이유만으로 만들면 안 된다. PDF 전체에 답이 없는지 검색과 사람 검수로 확인해야 한다.

## 13. 다중 근거 문항

상 난이도 문서에는 서로 다른 페이지나 섹션의 내용을 함께 찾아야 하는 문항을 포함한다.

```json
{
  "question": "기록 이관 과정에서 적용해야 하는 접근통제와 감사기록 요구사항을 함께 설명하라.",
  "reference_context_ids": [
    "상_1_p032_SER-004",
    "상_1_p048_SER-011"
  ],
  "reference_pages": [32, 48],
  "required_facts": [
    "접근권한 통제 방식",
    "감사기록 생성 및 보존 조건"
  ],
  "question_type": "multi_context",
  "answerable": true
}
```

다중 근거 문항은 Context Recall과 여러 청크를 이용한 답변 종합 능력을 평가하는 데 사용한다.

## 14. 최종 Golden set 스키마

```json
{
  "question_id": "상_1_q001",
  "document_id": "상_1",
  "question": "사용자 질문",
  "reference_answer": "원문에 근거한 기준 답변",
  "required_facts": ["핵심 사실 1", "핵심 사실 2"],
  "reference_context_ids": ["상_1_p032_SER-004"],
  "reference_pages": [32],
  "evidence_quotes": ["검수용 원문 근거"],
  "answerable": true,
  "question_type": "requirement",
  "difficulty": "high",
  "split": "test",
  "review_status": "approved",
  "reviewer": "human",
  "review_notes": "",
  "generator_model": "모델과 버전",
  "validator_model": "모델과 버전",
  "golden_set_version": "1.0"
}
```

## 15. 데이터 분리

90개 Pilot Golden set은 문서별 비율이 유지되도록 나눈다.

| 구분 | 문서당 문항 | 전체 문항 | 용도 |
|---|---:|---:|---|
| 개발셋 | 6 | 54 | 청킹, 검색기, 프롬프트 튜닝 |
| 검증셋 | 2 | 18 | 설정 및 모델 선택 |
| 최종 테스트셋 | 2 | 18 | 최종 성능 측정 |

최종 테스트셋은 청킹 크기, 검색 가중치, 프롬프트를 수정하는 데 사용하지 않는다. Golden set을 135~180개로 확장할 때도 최종 테스트셋 비중을 20~25% 정도 유지한다.

문항 단위 무작위 분리만 하면 유사 질문이 서로 다른 split으로 유출될 수 있다. 동일한 근거 또는 같은 요구사항에서 파생된 질문은 하나의 그룹으로 묶어 같은 split에 배치한다.

## 16. RAGAS 및 검색 평가 적용

Golden set 없이도 Faithfulness, Response Relevancy 등 일부 지표를 계산할 수 있지만, 실제 정답 및 정답 근거와 비교하려면 Golden set이 필요하다.

### 검색 평가

- Recall@5
- MRR@10
- nDCG@10
- ID 기반 Context Precision
- ID 기반 Context Recall
- RAGAS Context Precision
- RAGAS Context Recall
- 검색 응답시간 p50/p95

검색 품질은 우선 `reference_context_ids`와 검색된 청크 ID를 직접 비교한다. LLM 평가만으로 검색 성능을 판정하지 않는다.

### 답변 생성 평가

- RAGAS Faithfulness
- RAGAS Response Relevancy
- RAGAS Answer Correctness 또는 Factual Correctness
- `required_facts` 충족률
- 답변 불가능 문항의 정확한 거절률
- 출처 문서 및 페이지 인용 정확도
- 일부 표본에 대한 사람 평가

Faithfulness는 답변이 검색된 문맥에 근거하는지를 평가하지만, 검색 문맥 자체가 잘못됐는지는 보장하지 않는다. 따라서 Context Recall 및 정답 청크 ID 평가와 함께 사용한다.

## 17. 합성 데이터 사용 시 주의사항

LLM이 자동 생성한 문항만으로 최종 테스트셋을 구성하지 않는다. 합성 질문은 원문의 표현을 그대로 따르거나 실제 사용자 질문보다 쉽게 생성될 수 있다.

권장 용도는 다음과 같다.

- 사람이 작성하거나 강하게 검수한 문항: 핵심 Golden set
- LLM 생성 후 사람 검수한 문항: Golden set 확장
- 완전 자동 생성 문항: 회귀 및 스트레스 테스트

RAG 답변 생성 모델과 동일한 모델로만 평가하면 모델 편향이 생길 수 있다. 가능하면 생성 모델과 평가 모델을 분리하고, 한국어 RFP 문항 일부를 사람이 이중 평가해 LLM 평가 결과를 보정한다.

## 18. 완료 조건

Pilot Golden set은 다음 조건을 모두 충족해야 완료된 것으로 본다.

- [ ] 9개 PDF와 문서 ID의 실제 경로가 manifest에 확정됨
- [ ] 모든 청크에 문서·페이지·섹션·청크 ID가 존재함
- [ ] 문서당 10문항, 총 90문항이 준비됨
- [ ] 질문 유형별 목표 수량이 충족됨
- [ ] 답변 불가능 문항이 10~15% 포함됨
- [ ] 상 난이도 문서에 다중 근거 문항이 포함됨
- [ ] 모든 문항이 LLM 1차 검증을 통과함
- [ ] 모든 문항의 원문 근거를 사람이 최종 승인함
- [ ] 개발·검증·테스트셋 사이 중복과 근거 유출이 없음
- [ ] RAGAS 입력 필드와 ID 기반 검색 평가 필드가 준비됨

## 19. 권장 실행 순서

1. PDF manifest를 생성한다.
2. 9개 PDF를 페이지·요구사항 단위로 파싱한다.
3. 문서별로 질문 후보 근거 15~20개를 선정한다.
4. LLM으로 질문과 기준 답변 후보를 생성한다.
5. 별도 LLM으로 원문 기반 검증을 수행한다.
6. 자동 품질 검사로 필드 오류와 중복을 제거한다.
7. 사람이 문서별 10문항을 최종 승인한다.
8. 90개 Pilot Golden set을 분리한다.
9. BM25, Dense, Hybrid, Reranker를 동일한 Golden set으로 평가한다.
10. 실패 유형을 분석한 뒤 135~180문항으로 확장한다.

